server:
  port: 8083
spring:
  #application:
    #name: users-subgraph
  graphql:
    path: /graphql
  #datasource:
    #url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE
    schema:
      locations: classpath:graphql/
